---
- hosts: all
  gather_facts: yes

  vars:
    app_user: "{{ ansible_user }}"
    app_path: "/home/{{ ansible_user }}/asm-app"
    nodejs_version: "7.x"
    plymouth__theme: "AnotherSmartMirror"

  roles:
    - system
    - geerlingguy.nodejs
    - fubarhouse.yarn
    - application
    - splashscreen
